{
  "name": "rse-contracts",
  "version": "1.0.0",
  "description": "RSE Seat ERC-721 smart contracts",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:local": "hardhat run scripts/deploy.ts --network localhost",
    "deploy:base_sepolia": "hardhat run scripts/deploy.ts --network base_sepolia",
    "deploy:base": "hardhat run scripts/deploy.ts --network base",
    "verify:base": "hardhat verify --network base"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.1.0",
    "@types/node": "^22.0.0",
    "dotenv": "^17.3.1",
    "hardhat": "^2.22.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.0"
  }
}
